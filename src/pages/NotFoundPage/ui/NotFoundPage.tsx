import { PageWrapper } from "@/widgets/Page"
import styles from "./NotFoundPage.module.scss"

export const NotFoundPage = () => {
  return <PageWrapper className={styles.NotFoundPage}>PageNotFound</PageWrapper>
}
